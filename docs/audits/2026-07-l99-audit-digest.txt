================================================================================
AUDIT ?  GRADE: C+ — the doctrine and checkpoint-capture design are A-grade, but the loop as built loses human confirmations on every re-run, forks its central album contract b
SUMMARY: The Agentic Music Producer's OS has genuinely strong architectural doctrine — checkpoint capture over continuous observation, file-contract handoff between non-sharing Claude contexts, deterministic zero-dep scorers with printed reasoning, and hard human gates — but the loop as built jams in three places a daily producer would hit in week one. First, state management is broken: ingest-intake.mjs unconditionally rewrites briefs with guardian.confirmed:false on every scan and there is no processed ledger, so Frank's confirmations and edits are erased by the next run, and Suno's duplicate-title default causes silent brief overwrites because identity is keyed on title slug rather than sunoId. Second, the central data contract is forked: the Album OS spec and the built album.schema.json disagree on the manifest shape down to fully disjoint track-status enums, and the implemented brief omits t
-FINDINGS-
  [CRIT] The album manifest contract is forked: the spec and the built schema are incompatible
        docs/engineering/2026-07-album-os.md §2 specifies the manifest as {albumId, label slug, arcProfile, masteringProfile: sync-score|streaming, tracks[] keyed by inventoryId with canonTag/qc blocks and status: candidate|selected|culled|mastered|released, royaltyCa
  [CRIT] Re-ingest clobbers human decisions: guardian.confirmed and all Frank edits are erased on every run
        ingest-intake.mjs (line 507-508) unconditionally overwrites briefs/<slug>.yaml on every scan, always writing guardian.confirmed:false, and the brief-writer agent doc (music-brief-writer.md step 7) codifies this: 'Overwriting an existing brief for the same slug
  [CRIT] The learning loop — the thing that makes this 'intelligence' — has no concrete spec, no artifact, and never sees negative data
        The entire mechanism spec is one sentence in vibe-os/docs/engineering/2026-07-suno-ecosystem-bridge.md §3: 'the prompt agent reads recent take logs and biases toward the styles, BPMs, and phrasings that earned keep.' No keep-rate ledger schema, no aggregation 
  [MAJO] Track identity is keyed by title slug, not sunoId — Suno's own behavior guarantees collisions and silent data loss
        ingest-intake.mjs derives slug = slugify(title) (line 466) and writes briefs/<slug>.yaml. Suno generates two takes per prompt with identical titles as its default behavior — the single most predictable event in daily use. Two sidecars titled 'Ocean Chorus' pro
  [MAJO] Cross-repo integration is stdout regex scraping, and its failure mode is silent
        ingest-intake.mjs parseTaggerOutput (lines 120-137) parses tag-arcanea-guardian.mjs's human-readable console output with the regex ^(\d)\.\s+(\S.*?)\s+\(([^,]+),\s+(\d+)\s+Hz\)\s+—\s+score:\s+(\d+). The tagger has no --json flag (score.mjs got one; the tagger 
  [MAJO] The implemented brief omits fields the blueprint contract promises to five downstream modules
        AGENTIC-MUSIC-INTELLIGENCE-BLUEPRINT.md §3 defines the brief with track.frequencyHz, intent:{mood, audience}, and video:{type, energy-curve, key-moments}. The actual brief written by ingest-intake.mjs/music-brief-writer (see briefs/ocean-chorus.yaml) has none 
  [MAJO] Zero-signal Guardian proposals violate the system's own spec, and three incompatible confidence vocabularies coexist
        Album OS §3.2 says top score < 0.45 → 'Never force the nearest Guardian.' The built path does the opposite: ingest-intake takes tagger.top3[0] unconditionally (line 294), so a bare sidecar (REQUIRED_FIELDS is ['title'] only) yields a proposed Guardian chosen b
  [MAJO] Canon-fit is circular for system-generated tracks: the prompt is both the instruction and the evidence
        Album OS §3.1 deliberately closes the loop at the prompt ('embed the Guardian anchor... making future tracks self-describing at intake'). Combined with a scorer that reads only declared metadata, this means every track generated through the system's own prompt
  [MAJO] No reflection layer: after an album ships, learnings have no write target and no reader
        The module 12→1 learning edge is a dotted mermaid line in SYSTEM.md with no mechanism behind it. Nothing specifies where post-release knowledge goes: no retro artifact per album, no performance-data (streams/engagement) ingestion contract, and Album OS §7 Q8 (
  [STRE] Checkpoint capture over continuous observation is the right architecture, correctly reasoned
        The blueprint §1 boundary map (Claude Code / Claude-in-Chrome / Playwright, no shared context, file-only handoff) and the capture-at-approval rule are genuinely sound: they convert the most expensive agent behavior into a few-hundred-token event at exactly the
  [STRE] Deterministic zero-dep tooling with printed reasoning and hard human gates
        tag-arcanea-guardian.mjs, album-builder.mjs, score.mjs, and ingest-intake.mjs are all zero-dependency, no-network, no-LLM, and every score prints its per-component breakdown ('exact deltas, never adjectives'). The never-auto-bind / never-auto-publish disciplin
  [STRE] The excellence review is real self-criticism, not theater
        EXCELLENCE-REVIEW-2026-07.md correctly identifies audio-blindness, the mastering overclaim, the missing stem stage, the unbuilt learning loop, and ICP overclaim — with concrete, cheap, correctly-prioritized fixes (librosa/ebur128 at intake as P0, Demucs, Match
-RECOMMENDATIONS-
  (hours) Reconcile the album manifest to ONE contract: either upgrade schemas/album.schema.json to the Album OS §2 shape (status lifecycle enum, masteringProfile, inventoryId, canonTag/qc blocks) or amend the spec to bless the built schema — then make album-builder.mjs validate whichever wins, before any Phase-1/2 code is written against the spec.
        why: Every downstream module (curate, master-gate, release) will be coded from the spec and will fail against real manifests; the status enums are already fully disjoint. This is the ve
  (days) Separate machine-generated briefs from human decisions: move guardian confirmation (and any Frank edits) into an append-only decisions file the ingest planner never writes (e.g. briefs/<slug>.decisions.yaml or a single decisions.jsonl), and add a processed-sidecar ledger keyed by sunoId so re-runs skip already-briefed tracks.
        why: Today any intake re-run resets guardian.confirmed to false and erases Frank's edits — the human gate, the system's central design commitment, is destroyed by its own idempotency cl
  (hours) Key track identity by sunoId everywhere (brief filename or a sunoId field used for dedupe, album sequence refs, processed ledger); fall back to title only when sunoId is absent, with an explicit collision error instead of silent overwrite.
        why: Suno emits multiple takes with identical titles by default; the current title-slug keying silently destroys the earlier brief on the most common daily event.
  (hours) Add --json output to tag-arcanea-guardian.mjs (score.mjs already has the pattern) and make ingest-intake.mjs and music-brief-writer consume it; treat an empty result as a hard error, not an available=true/empty-top3 success.
        why: Cross-repo coupling through regex-scraped human stdout fails silently on any format change — the exact class of shape bug the ecosystem just shipped.
  (days) Specify the learning loop concretely before building it: JSON Schemas for the sidecar, take-log (including kill/rework records and the take-log→sidecar mapping), and a keep-rate ledger (e.g. memory/take-stats.json) computed by a deterministic aggregator; require every prompt-pack to cite which stats biased it.
        why: The loop is currently one sentence, two docs disagree on the take-log shape, no example file exists anywhere, and negative verdicts — the data a learning loop needs most — never re
  (hours) Implement Album OS §3.2 in the built path: below the unmatched threshold propose NO Guardian (proposed: null, route releases-experimental), add the margin-over-runner-up confidence tier, and pick one confidence vocabulary (tier string plus raw score) used identically in brief, spec, and eval output.
        why: The built code proposes alphabetically-first Guardians on zero signal, directly violating the spec's 'never force the nearest Guardian', and three incompatible confidence scales gu
  (days) Add the reflection checkpoint: a per-album retro artifact (albums/<id>/retro.json + prose) capturing keep-rate, gate pass rates, the scoring weight vector used, and engine A/B outcomes — with a named reader (the ideation step's prompt-pack generator loads the last N retros), and route a summary atom to the existing memory substrate.
        why: Module 12→1 is currently a dotted line with no write target and no reader; without a structural home, what-was-learned evaporates exactly like pre-brief approvals used to.
  (days) Add a pre-generation self-eval gate: run canon-fit on each prompt-pack entry's declared parameters, store the predicted score in the pack, and after the session compare predicted scores against Frank's keep verdicts — alongside the P0 audio-measured features at intake that break the prompt-echo circularity.
        why: Canon-fit is currently tautological for system-generated tracks (the prompt scores itself); predicted-vs-realized calibration is the cheapest way to make the scorer accountable to 
================================================================================
AUDIT ?  GRADE: B-: strong, internally consistent prompt craft in the four FrankX production agents, undercut by a broken hardcoded input path with no fallback in the most-used
SUMMARY: Read the four FrankX Pillar-2 music agents (brief-writer, album-orchestrator, producer, prompt-architect), the MIS swarm-orchestrator and lyric-writer hub personas, the /album command, and the suno-prompt-architect skill, and verified their claims against the actual filesystem where possible. The four FrankX agents share a disciplined, identical 9-section template with explicit failure paths, human gates, and matching JSON output contracts -- this is the strongest craft in the set and would survive a weaker model well. But two concrete, filesystem-verified defects sit in the two most consequential files: music-suno-prompt-architect.md (the most-used agent) hardcodes a Read path to a skill file that does not exist anywhere on this machine, contradicting its own frontmatter tool list, with no fallback defined; and music-album-orchestrator.md (the most complex agent) cites a Smoke-eval fixt
-FINDINGS-
  [CRIT] music-suno-prompt-architect.md points at a skill file that doesn't exist, with no fallback
        Inputs section requires `~/.claude/skills/suno-prompt-architect/SKILL.md` (user-home path) and Process step 1 says 'Read the skill. Load ~/.claude/skills/...'. Verified: no ~/.claude/skills directory exists anywhere on this filesystem (checked /home/user/.clau
  [MAJO] music-album-orchestrator.md's own Smoke eval fixture does not exist
        The Smoke eval section names tests/fixtures/music-album-orchestrator/smoke.mjs as the functional contract required for 'shipped' status. Verified via filesystem search: no such directory exists under FrankX/tests/fixtures, while the three sibling agents in the
  [MAJO] No shared prompt-quality standard across the two repos -- two incompatible conventions bridged with no adapter
        The four FrankX agents (brief-writer, album-orchestrator, producer, prompt-architect) share an identical 9-section template (Purpose/Triggers/Inputs/Process/Outputs/Integration/Smoke eval/Anti-patterns/Model choice/Voice check) and frontmatter schema (name, de
  [MAJO] lyric-writer.md has no tool scoping and no machine-parseable output contract
        Unlike every FrankX music agent (each scoped to 2-4 named tools), lyric-writer.md's frontmatter has no `tools:` field at all. Combined with `portable: true` this reads as an intentional 'paste into any LLM' persona doc, but that distinction is never stated in 
  [MINO] Two consuming agents describe different CLI signatures for the same shared tool, neither cross-checked against the other
        music-brief-writer.md step 2 invokes tag-arcanea-guardian.mjs with --title/--bpm/--mode/--frequencyHz only. music-album-orchestrator.md step 3 additionally passes --vocalPosture. I read the actual script (music-intelligence-systems/tools/tag-arcanea-guardian.m
  [MINO] music-producer.md's create-wizard dispatch target is listed but never appears in the numbered Process
        Integration section lists 8 dispatch targets including music-create-wizard ('optional path for end-user-facing guided flows'), but the numbered Process (stages 1-8) never dispatches it -- only catalog-indexer, suno-mastery, prompt-architect, mastering-qc, lice
  [MINO] swarm-orchestrator.md's 25-row routing table plus 5 disambiguation rules has no worked example for a genuinely ambiguous compound ask
        The file states the disambiguation rules abstractly (personal-catalog vs. operated-label vs. public-vertical; ad hoc vs. curriculum; persona vs. MCP vs. CLI vs. command; portable:false; compound-ask fan-out) and gives one compound-ask example for fan-out (rule
  [STRE] Strong human-gate discipline across the FrankX music-agent family
        brief-writer, album-orchestrator, producer, and the /album command all independently enforce the same specific rule -- 'guardian.confirmed is never set true by this agent', tagger proposes/Frank confirms, QC fail always holds a track back, licensing needs-revi
  [STRE] Machine-parseable output contracts with matching status enums across the FrankX family
        All four FrankX agents emit an identical-shape JSON footer (status enum, outcome object, memory_ids array) and a matching human-readable stdout template with a NEXT ACTION section. This means a weaker model doesn't need judgment to know when a flow succeeded/p
  [STRE] swarm-orchestrator.md's explicit anti-hallucination clause paired with a deterministic backstop
        The file states 'never invent an agent, skill, or tool. If the registry doesn't have something, say so and name the closest real match' and then gives the executing model a machine-checkable escape hatch (tools/swarm-router.mjs, deterministic keyword-overlap s
-RECOMMENDATIONS-
  (hours) Fix music-suno-prompt-architect.md's broken input path: change ~/.claude/skills/suno-prompt-architect/SKILL.md to the actual repo-relative path (.claude/skills/suno-prompt-architect/SKILL.md), or drop the hardcoded Read instruction and tell the model to invoke the Skill tool by name ('suno-prompt-architect') since Skill is already listed in the frontmatter tools. Add an explicit 'skill-unavailable, proceed with defaults' fallback matching every sibling agent's pattern for a missing dependency.
        why: This is the most-used Pillar 2 agent and its single most load-bearing input currently points nowhere on this filesystem, with zero documented recovery path.
  (hours) Either build the missing tests/fixtures/music-album-orchestrator/smoke.mjs to match the three-scenario contract already specified in the file's Smoke eval section, or remove the 'shipped' claim/Smoke eval section until it exists.
        why: An unfalsifiable verification claim inside the prompt itself undermines trust in every other claim the file makes about its own correctness, and it's the one agent in the family mi
  (days) Bring lyric-writer.md (and film-sync-composer.md, the other hub persona docs swarm-orchestrator routes to directly) up to the FrankX agent template where they're meant to interoperate with it: add explicit tools: scoping, a JSON output contract, a memory recall/persist step, and an Anti-patterns section. Where the doc is deliberately meant to stay a portable, non-Claude-specific persona (portable: true), say so explicitly inside the file and define what changes when it's loaded as a Claude Code subagent vs. pasted into another LLM.
        why: The file is silently missing every mechanism the rest of the system relies on for safe composition, and nothing explains whether that's deliberate or an oversight.
  (days) Write one short cross-repo prompt-quality standard (a single markdown spec, not a new template per repo) that both FrankX's 9-section agent format and MIS's Role/Expertise/Boundaries format can be checked against -- minimum required sections, frontmatter schema, and what an output contract must contain for any agent another agent might dispatch programmatically.
        why: Two independently-invented conventions currently coexist with no reconciliation, and the routing layer (swarm-orchestrator) already assumes cross-repo dispatch works seamlessly whe
  (hours) Add a one-line cross-check note (or a shared constants file) so any agent that shells out to tag-arcanea-guardian.mjs cites the same flag set, sourced from the script's own usage string rather than re-typed independently in each consuming agent file.
        why: brief-writer and album-orchestrator already drifted on this once; the next agent added to shell out to this tool has no single source of truth to copy from.
================================================================================
AUDIT ?  GRADE: C — a well-designed hub taxonomy undermined by a four-way collision on the single name (Vibe OS) that carries the paid funnel, an unsettled flagship name, and a
SUMMARY: The naming stack is a pile with a coherent core buried inside it. The hub (/home/user/music-intelligence-systems) is genuinely well-architected — README.md, SYSTEM.md, and docs/AUDIENCES.md route six ICPs cleanly under a "compose, don't duplicate" rule — but the commercial layer contradicts itself at the exact point money changes hands: frankx.ai/music/templates upsells Vibe OS as "the paid companion... the full Suno Music Mastery toolkit," while data/products.json defines /products/vibe-os as "Vibe OS: Creative State Management," badge "FREE TOOL," a mood/energy tracker with a Notion template and zero music content; meanwhile /vibe titles Vibe OS "Multi-Agent Music Creation & AI Orchestration" and the vibe-os repo README calls it "Research-Backed Music for State Change" with "Frequency Healing." That is four incompatible definitions of the one name that carries the funnel. The flagship 
-FINDINGS-
  [MAJO] Naming inventory: nine names, six routes, three registers
        Inventory as found on disk: (1) 'Music Intelligence System' — hub repo /home/user/music-intelligence-systems (repo slug is plural, every doc title singular), public page app/music-intelligence/page.tsx; claims to be 'the open hub for AI-era music creation.' (2
  [CRIT] Vibe OS means four different things and the paid funnel dead-ends on the contradiction
        frankx.ai-vercel-website/app/music/templates/page.tsx (lines 286-300) sells the upgrade: 'Vibe OS is the paid companion to these templates: prompt packs across genres, emotion mapping, and production checklists' with CTA to /products/vibe-os. But data/products
  [MAJO] Flagship name is unsettled: 'Agentic Music Producer's OS' vs 'Agentic Music Intelligence System'
        music-intelligence-systems/SYSTEM.md line 1 titles the whole thing 'the Agentic Music Producer's OS, in one map', while the umbrella doc it points to, docs/AGENTIC-MUSIC-INTELLIGENCE-BLUEPRINT.md, is titled 'Agentic Music Intelligence System — Master Blueprint
  [MAJO] Science register and mythic register contradict each other on Solfeggio/frequency claims
        music-intelligence-systems/README.md line 85: 'anecdotal traditions (e.g. solfeggio) are labeled as such'; app/music-intelligence/page.tsx (research section) explicitly hedges binaural-beat claims as 'open questions rather than selling points.' But Arcanea CAN
  [MAJO] ai-music-academy README violates the FrankX brand voice wholesale
        /home/user/ai-music-academy/README.md opens with 'Revolutionizing Music Creation', 'the world's first Agentic Music Creation Platform', 'our revolutionary Six-Agent Orchestra', emoji-dense headers, and vision copy ('a world where creativity knows no bounds'). 
  [MINO] OS-suffix inflation dilutes every OS
        Within this one domain: Vibe OS, Album OS, Agentic Music Producer's OS — alongside the wider ecosystem's ACOS/Agentic Creator OS and Library OS. When everything is an OS, the suffix stops signaling 'the flagship system' and starts signaling 'a markdown file'. 
  [MINO] Hub name singular/plural drift and duplicate internal domain names
        GitHub slug is music-intelligence-systems (plural); README.md, SYSTEM.md, and the website page all say 'Music Intelligence System' (singular, abbreviated MIS). Additionally Starlight hosts both a 'sound-intelligence' vertical (6 agents, 30 commands) and a 'mus
  [MINO] Catalog-size claims drift across brand surfaces despite a Metrics Truth Rule
        frankx.ai-vercel-website/CLAUDE.md says '500+ AI songs'; FrankX/CLAUDE.md and music-intelligence-systems/SYSTEM.md say '12k+ track catalog'; the Album OS doc measures data/music-asset-registry.json at 61 tracks (verified 2026-07-07 per that doc). Three orders 
  [STRE] Strength: the hub's audience routing is real brand architecture
        docs/AUDIENCES.md maps six ICPs (producers, film/sync, orchestras, creators, educators, labels) to a named first agent, MCP tool, and free entry point each, and README.md's 'Who is it for?' table mirrors it. This is the skeleton of a correct masterbrand-to-pro
  [STRE] Strength: the two live public pages are on-voice and mutually coherent
        app/music-intelligence/page.tsx and app/music/templates/page.tsx are understated, evidence-hedged ('We treat those as open questions rather than selling points'), correctly cross-linked, and use the free-templates-first CTA discipline the FrankX brand contract
  [STRE] Strength: an explicit brand-register separation rule already exists in the ecosystem
        Starlight-Intelligence-System/CLAUDE.md's naming note (2026-05-03) formalizes exactly the needed pattern: 'Luminor stays Arcanea-mythic; Starlight is the substrate register' — same function, different canon attribution, documented. Extending this one existing 
  [MAJO] Recommended naming architecture and the selling spine (deliverables 3-5)
        Masterbrand: FrankX (person/site). Public umbrella: Music Intelligence System — keep, it owns /music-intelligence and the SEO; one-liner: 'The open hub for AI-era music creation — research, portable agents, and tooling that take a track from idea to released a
-RECOMMENDATIONS-
  (hours) Rewrite the /products/vibe-os record in frankx.ai-vercel-website/data/products.json to be the music state-change product (paid Suno toolkit: prompt packs, emotion mapping, production checklists) that /music/templates already promises; keep the URL, replace the 'Creative State Management' energy-tracker copy entirely.
        why: This is the only paid conversion point in the music funnel and it currently contradicts the page that feeds it ('paid companion' vs 'FREE TOOL' mood tracker). Every template visito
  (hours) Pick one flagship name — recommend 'Music Intelligence System' as the public umbrella with 'the Agentic Music Producer's OS' as its positioning tagline — and align SYSTEM.md, the blueprint title, and /vibe's page title to it.
        why: Two internal flagship names and a third public one means the system cannot be referred to, linked to, or searched for consistently; the /vibe page currently spends the Vibe OS name
  (hours) Apply the existing Starlight brand-register rule to music: Solfeggio/frequency-canon language lives only on Arcanea label surfaces (with a one-line 'canon, not clinical claim' disclosure); strip 'Frequency Healing' from vibe-os/README.md or rewrite it to match the hedged research posture.
        why: The hub's credibility rests on 'research, not vibes-about-vibes' (README.md); an adjacent repo selling binaural/solfeggio healing flat contradicts it and hands skeptics the whole s
  (hours) Rewrite ai-music-academy/README.md in the FrankX voice: delete 'world's first', 'Revolutionizing', 'revolutionary', and the vision-statement guru copy; lead with the 4-tier curriculum and the portable personas.
        why: It is a registered brand surface (SYSTEM.md repo map) in direct violation of the brand contract's DON'T list and the Metrics Truth Rule.
  (days) Add a BRAND-MAP.md to the hub repo: one table listing every name, its single assigned meaning, its register (science/product/mythic), its ICP, its canonical URL, and its one-liner — and make it the gate for any new name.
        why: The pile grew because there was no single file that says what each name means; the hub already has this pattern for repos (ECOSYSTEM.md) and agents (registry), just not for names.
  (hours) Retire 'Album OS' as an outward-facing name; refer to it publicly as the album pipeline within MIS, keeping the internal spec filename as-is.
        why: A proposed design doc carrying the OS suffix competes with the flagship and Vibe OS for the same suffix real estate and inflates the count of things a newcomer must disambiguate.
  (hours) Reconcile the song-count claim (500+ vs 12k+ vs 61 registered) per the Metrics Truth Rule: one number with a last-verified date in metrics/current.json, referenced everywhere else.
        why: The catalog size is the core credibility claim behind the whole music brand; three-order-of-magnitude drift across CLAUDE.md files will eventually surface publicly.
================================================================================
AUDIT ?  GRADE: D+ — real underlying agent/research/portability IP exists, but the music-specific go-to-market is pre-revenue: no paid SKU for the actual Music Intelligence Sys
SUMMARY: The real offer ladder today is: free (Vibe OS "free forever" per products.json, 9 Suno templates, Music Lab) then a jump straight to generic creator-economy paid SKUs ($19-$97 self-serve: Aurora UI Kit, Suno Prompt Library, Creative AI Toolkit, Content Engine) and high-ticket consulting ($297-$4,800+: Creator Lab OS, Creation Chronicles, Generative Creator OS) — none of which is actually the Music Intelligence System. Per ICP (AUDIENCES.md's six personas), only Independent Producers have anything buildable and buyable in under four weeks (a prompt-pack SKU cloned from the existing Gumroad pattern); Film/Sync Composers, Orchestras, Labels/A&R, and Educators all dead-end at GitHub docs or an unenrollable academy with no backend. The gaps between repo and product are severe: the quickstart's own first command (`pip install vibe-os-mcp`) 404s on PyPI; the academy's financial-projections.md d
-FINDINGS-
  [CRIT] The flagship self-serve quickstart is broken at step one
        docs/GETTING-STARTED.md (Path 2, lines 22-34) tells users to run `pip install vibe-os-mcp`. Verified live: `pip index versions vibe-os-mcp` returns 'No matching distribution found' and `curl https://pypi.org/pypi/vibe-os-mcp/json` returns HTTP 404 — the packag
  [CRIT] No paid SKU exists for the Music Intelligence System / Vibe OS itself
        data/products.json (lines 190-298) lists vibe-os as badge 'FREE TOOL', primaryPrice 0, with FAQ stating 'Vibe OS is completely free... Any future premium features would be add-ons, not requirements.' app/music-intelligence/page.tsx has zero purchase CTA anywhe
  [MAJO] Academy financial plan describes a business that provably does not exist
        docs/business/financial-projections.md projects Basic/Premium/Pro subscriptions ($29.99/$59.99/$99.99/mo), enterprise seats, VR studio access, and marketplace commission revenue. roadmap/DEFERRED.md (music-intelligence-systems) and docs/ECOSYSTEM-ALIGNMENT.md 
  [MAJO] The single most differentiated sellable workflow depends on unbuilt code
        AGENTIC-MUSIC-INTELLIGENCE-BLUEPRINT.md's own productization table (section 6, lines 169-181) maps 'approve -> full campaign one-click kit' (modules 8+9) as the Creator-facing product. Module 8 itself is flagged 'Not built — priority 1' (section 2 table, line 
  [MAJO] The proof album has zero finished canon-compliant tracks
        albums/arcanea-vol-1/album.json and README.md: 3 tracks are 'legacy... off-canon' pop-rock/arena-rock tracks predating the Guardian-binding discipline (no guardian field), and the other 3 are 'planned Guardian-bound placeholder slots' not yet generated. There 
  [MINO] No commercial-use/license terms on the one real music paid product
        Suno Prompt Library Pack ($27, products.json lines 718-845) has no license field and no FAQ answer on commercial use of tracks generated from the prompts. Contrast Aurora UI Kit (line 938-940), which explicitly states 'MIT license... unlimited personal and com
  [MINO] Educator ICP entry point leads to a product that can't be bought or enrolled in
        AUDIENCES.md (section 5, lines 67-78) sends 'Music Educators' to ai-music-academy with '4 tiers: Starter -> DJ -> Producer -> Genius' as the entry point. ECOSYSTEM-ALIGNMENT.md confirms curriculum content and tier mapping exist, but DEFERRED.md confirms there 
  [STRE] The blueprint's productization and phase-gating discipline is a real strength
        AGENTIC-MUSIC-INTELLIGENCE-BLUEPRINT.md section 4 sequences monetization strictly behind working phases with explicit unblock conditions ('one approved track -> auto-generated cover + video from its brief' before Phase 2), and section 5 gives an honest go/no-g
  [STRE] The portability/export engine genuinely reaches non-Claude-Code buyers today
        docs/PORTABILITY.md documents 13 agents x 4 formats = 52 pre-generated, committed exports (Claude Projects, Custom GPTs, Gemini Gems, generic system prompts) that work via simple copy-paste with no install step. Unlike the broken pip path, this is a working, l
  [STRE] A working, Gumroad-integrated self-serve checkout pattern already exists and could be cloned for music
        products.json shows a proven $19-$97 digital-product ladder (Aurora UI Kit $19, Suno Prompt Library $27, Creative AI Toolkit $47, Agentic Content Engine $97) with real Gumroad checkout links, guarantees, bonuses, and delivery manifests. This pattern is a templ
-RECOMMENDATIONS-
  (days) Ship a narrow $27-37 'Vibe OS Producer Pack' via the existing Gumroad checkout pattern (clone Suno Prompt Library Pack structure), targeting only the Independent Producers ICP.
        why: Fastest path from repo to paid outcome; reuses proven checkout, refund, and delivery infrastructure already live in products.json; matches an established market price point (Splice
  (hours) Fix or remove the `pip install vibe-os-mcp` instruction in docs/GETTING-STARTED.md.
        why: It is the literal first command in the '10 minute' quickstart and returns a 404 today, killing trust for the highest-intent technical segment before they reach anything else.
  (hours) Delete or clearly relabel docs/business/financial-projections.md as speculative/future-state, separate from any current pricing decision.
        why: It currently reads as a real business model for a subscription SaaS that has no auth, DB, or payment wiring — a documented claim that could mislead investors, partners, or Frank hi
  (days) Build module 8 (approval-to-brief hinge) before marketing 'the system' as a product, per the blueprint's own phase gate.
        why: It's the acknowledged missing piece that makes five downstream modules (cover, video, publish, social, monetize) actually connect; selling before it exists is selling vaporware.
  (hours) Add one explicit commercial-use license sentence to the Suno Prompt Library Pack offer.
        why: Producers and labels are exactly the buyers who ask this question because their output gets synced or sold; Aurora UI Kit already sets the pattern to copy.
  (weeks) Finish at least one canon-compliant Arcanea track (move one 'planned' slot in album.json to 'published') before building any case-study or landing-page asset around the album.
        why: There is currently nothing finished to point proof-of-concept marketing at; the album is the intended flagship case study per the blueprint's own logic.
  (days) When ready to sell a higher-ticket 'Album OS' workflow, price it in the existing $297-997 Creator Lab OS band rather than inventing new SaaS subscription tiers.
        why: Matches the buyer archetype (serious indie artist/label wanting a repeatable system) already validated by an existing offer, and avoids recurring billing without recurring delivere
================================================================================
AUDIT ?  GRADE: C- — the tooling doctrine and deferral discipline are genuinely strong, but the data layer contradicts its own source-of-truth claims: 6+ live shapes, 3 state v
SUMMARY: The tooling layer is the healthiest part of this system and the data layer is the sickest. Track data verifiably lives in at least six concurrent shapes across three repos — music-asset-registry.json (61 tracks, stale since 2026-03-04, with a declared source file that does not exist), suno-catalog.json (817 tracks, different key name), review-queue.json, catalog.csv (121 rows, 35 cols), track-state.json (self-declared single source of truth), inventories/frankx/music.json, and album manifests — with three incompatible status vocabularies and metrics denormalized into snapshots with no freshness fields. The most damning single fact: track-state.json IS the right canonical spine and its generator (build-track-state.mjs) already exists, yet the /studio/music/ops dashboard that should display it reads the three raw sources instead, and Starlight's master.csv 'canonical catalog' contains one 
-FINDINGS-
  [CRIT] At least six live track shapes with three conflicting state vocabularies and no enforced canonical key
        Field-level fragmentation confirmed: FrankX/data/music-asset-registry.json (sunoId, metrics{plays,likes}, assetRefs{}, status: published/draft/archived), data/music/suno-catalog.json (key is `id` not `sunoId`, flat plays/likes, styleTags as a single string), d
  [CRIT] The declared single source of truth is bypassed by its own dashboard, and the registry's declared source file does not exist
        track-state.json's _description says 'single source of truth for where every track is in the pipeline' (generated by scripts/music/build-track-state.mjs, merging suno-catalog + review-queue + release ledger — exactly the right pattern). But grep of app/studio/
  [MAJO] Starlight master.csv is dead scaffolding while the real ledger forked in FrankX — MIGRATION.md targets the wrong file
        Starlight-Intelligence-System/verticals/music-is/catalog/master.csv contains exactly 2 lines: the header and an EXAMPLE seed row ('replace before Phase 1 first real intake', dated 2026-04-29). The real ledger with 121 rows lives at FrankX/data/music/catalog.cs
  [MAJO] The hub's track schema canonizes the stalest shape and is enforced against nothing
        music-intelligence-systems/schemas/track-metadata.schema.json says it is 'derived from the FrankX data/music-asset-registry.json catalog format' — i.e., the 61-track registry last updated 2026-03-04, four months stale and superseded by suno-catalog (817 tracks
  [MAJO] Cross-repo copy-sync duplication of the ledger with no drift guard
        catalog.csv (122 lines) and music-asset-registry.json exist byte-similar in both /home/user/FrankX/data/ and /home/user/frankx.ai-vercel-website/data/ (verified: identical row counts and identical _stats/_lastUpdated). They are currently in sync only because t
  [MINO] Denormalized metrics snapshots with no per-field freshness, contradicting the ecosystem's own Metrics Truth Rule
        plays/likes are copied into review-queue.json (at review time), track-state.json (at build time, plus computed engagement), and music-asset-registry.json (frozen since March: 'Arcanea (light me up)' shows 71 plays there vs live suno-catalog numbers). Only file
  [MINO] Speculative integration fields shipped before any integration existed
        music-asset-registry.json carries googleDriveFolder and notionPageId on all 61 tracks and _stats confirms withDriveLinked: 0, withNotionLinked: 0 — designed in March, never populated once in four months. This is direct in-repo evidence for the anti-hype answer
  [MAJO] Audio-analysis stage has no storage substrate: audio lives on third-party CDNs, columns exist but are empty
        Every audioUrl in suno-catalog points at cdn1.suno.ai and every registry audioUrl at Vercel blob storage — librosa/Demucs/Matchering (excellence review P0/P1) need local files. Someone already anticipated this: catalog.csv gained audio_path and beatgrid_path c
  [STRE] Strength: build-track-state.mjs is the correct canonical-spine pattern, already built
        track-state.json's design — one generated per-track spine keyed by sunoId, merging the scrape feed, review events, and release ledger, with human stage/flag decisions preserved across rebuilds — is exactly the right architecture for this scale. The fix for the
  [STRE] Strength: MEDIA-TOOLING-DOCTRINE's native-default and new-engine rules are real anti-hype governance
        The doctrine routes by cost/determinism/volume/durability with concrete evidence (it cites Higgsfield MCP disconnecting repeatedly during its own authoring session as the durability argument), reserves credit-metered engines for three named cases, and bans add
  [STRE] Strength: DEFERRED.md records why integrations are NOT built, with unblock conditions
        Suno API (no public API as of 2026-06-22, integration point pre-named), Supabase/academy backend (deferred until explicit commitment, blocking conditions listed), distribution automation (deferred as external side effect per hard-stop policy). This file is the
-RECOMMENDATIONS-
  (hours) Declare track-state.json the canonical spine (keyed by sunoId) and point app/studio/music/ops/page.tsx and the catalog page at it instead of the three raw files; treat suno-catalog.json as an immutable scrape feed, review-queue.json as an event log, and catalog.csv as the release-ledger view.
        why: The spine and its builder (scripts/music/build-track-state.mjs) already exist; the fragmentation persists only because consumers bypass it. This is a rewiring, not a build.
  (days) Unify the three status vocabularies into the track-state 9-stage enum, update schemas/track-metadata.schema.json to describe the spine (not the March registry shape), and add a CI/pre-commit validation step; fix or delete the dangling _source.musicIndex reference in music-asset-registry.json and mark that file deprecated/generated.
        why: A schema certifying the stalest shape is worse than none; three lifecycle enums for the same track guarantees agents write inconsistent state.
  (hours) Retire Starlight's verticals/music-is/catalog/master.csv (or reduce it to a pointer doc) and update MIGRATION.md to target FrankX/data/music/catalog.csv with its extended 35-column schema.
        why: master.csv holds one example row while the real ledger has 121 rows and a forked schema; every future migration session that follows MIGRATION.md as written will populate a dead fi
  (hours) Do NOT adopt Supabase or Notion as the track store; add SQLite only as a locally generated, gitignored index built from the JSON spine, and only when Tier-3 (12k) ingest actually begins.
        why: No multi-user runtime, no auth, no web write-path exists (DEFERRED.md defers the only backend that would need Supabase); the active working set (~1,000 tracks) is trivially handled
  (days) Build the audio substrate for the analysis stage: local cache directory outside git (12k tracks = 50-100GB), a download step in ingest, populate the already-added audio_path column, and write measured fields (bpm_measured, key_measured, lufs_measured, mismatch flag) into the spine via librosa + ffmpeg ebur128; add Demucs v4 stems and Matchering per the excellence review P0/P1 table.
        why: Every audioUrl currently points at cdn1.suno.ai or Vercel blob storage — librosa/Demucs/Matchering cannot run without local files, and the pipeline is grading declared metadata it 
  (hours) Handle post-release analytics as a manual-export import script (DistroKid bank/stream CSV, Spotify for Artists CSV) writing dated snapshot rows into the spine — not a Spotify MCP.
        why: DistroKid has no API (consistent with DEFERRED.md's pattern) and Spotify's public API does not expose your per-track stream counts (that is Spotify for Artists, which also has no p
  (hours) Add a data-sync drift guard to the two-repo deploy flow: a checksum or generated-header check that fails merge:gate when FrankX/data/music/* and frankx.ai-vercel-website/data/music/* diverge.
        why: The prod copies of catalog.csv and music-asset-registry.json are identical today only by recency of manual sync; the first missed sync silently ships stale catalog state to frankx.
  (hours) Skip building any new dashboard; after the spine rewiring, add a single stage-funnel view (discovered → released counts from track-state._stageCounts) to the existing ops page if anything.
        why: /studio/music/ops (485 lines) and /studio/music/catalog (364 lines) already render this domain; the deficiency is truthfulness of what they read, not absence of a surface.
================================================================================
AUDIT ?  GRADE: C+ — a genuinely well-architected registry and routing design (swarm-orchestrator, human-gated brief hinge, deterministic eval harness) undercut by concrete, ve
SUMMARY: Across the six repos the ecosystem is larger than the registry admits: registry/agents.json's 32-entry canonical list (FrankX 9 Pillar-2 + hub 5 + Starlight 13 across sound-intelligence/music-is + vibe-os 1 + academy 3 + ACOS 1) is real and matches files on disk, but FrankX/.claude/agents/ also contains two additional, undocumented, name-colliding "Music Producer" agents that the registry's own "don't duplicate" promise says shouldn't exist. Command-surface inventory: FrankX has ~13 music/mv-* commands, Starlight has 30 /sound-* plus 8 /music-* commands (registry says 7, one is undercounted), and the hub ships a genuinely good meta-router (swarm-orchestrator.md) with explicit disambiguation rules and portable/status flags — this is the strongest part of the system and worth preserving as the front door for ambiguous asks. Against that strength sit concrete, verifiable defects: /create-mu
-FINDINGS-
  [CRIT] Parallel asset agents write back to shared catalog files with no locking — a file-level version of the exact clobber pattern the ecosystem's own CLAUDE.md warns about
        WORKFLOW.md Loop B states explicitly: 'Cover / video / master run in parallel once a brief exists ... none depends on the others' and frames this as a 'swarm pattern.' But mv-canvas.md says 'Write canvas_path back to the catalog row', mv-artwork.md says 'write
  [MAJO] /create-music command is not wired to the @music-create-wizard agent that claims to own it — two disconnected implementations of the same front door
        music-create-wizard.md's description says it 'Auto-invokes when Frank says ... or runs /create-music', and music-intelligence-systems/agents/swarm-orchestrator.md's routing table states 'Wizard is the persona; the command is the literal invocation surface.' Bu
  [MAJO] Two undocumented legacy agents both named 'Music Producer' collide with the canonical music-producer orchestrator and are absent from the hub registry
        FrankX/.claude/agents/frequency-music-production.md and FrankX/.claude/agents/music-production.md both carry frontmatter `name: Music Producer` — identical to the canonical Pillar-2 orchestrator (id `music-producer`, description 'the Opus composer that closes 
  [MAJO] .claude/ command and agent files — declared private-only — are duplicated byte-for-byte into the production repo
        Both FrankX/CLAUDE.md and frankx.ai-vercel-website's own sync table say `.claude/` 'stays private' and does not sync to production. Yet diff shows FrankX/.claude/commands/create-music.md, FrankX/.claude/agents/frequency-music-production.md, and FrankX/.claude/
  [MAJO] The swarm-orchestrator router and registry/tools.json have already drifted from the live registry counts
        swarm-orchestrator.md (last_updated: 2026-07-06) states 'the current 31 agents / 10 tools in the registry' in its routing-table header, but registry/agents.json (updated 2026-07-06, same date) actually holds 32 entries and registry/tools.json (updated 2026-07-
  [MINO] SYSTEM.md/WORKFLOW.md mark module 8 (brief-writer) as 'NOT BUILT' while a working agent and a passing fixture brief already exist
        SYSTEM.md and WORKFLOW.md (both mtime 2026-07-08 09:33) both label module 8 with a `gap`/'NOT BUILT' classification and describe the brief-writer as the roadmap's next build. But FrankX/.claude/agents/music-brief-writer.md is a fully-specified agent (mtime sam
  [MINO] Three-tier Arcanea canon copy chain has no automated freshness check between tiers
        The 10-Guardian roster exists in three places: an external file the docs cite as ground truth (`C:\Users\frank\Arcanea\.arcanea\lore\CANON_LOCKED.md`, unverifiable from these repos), a copy at Starlight-Intelligence-System/verticals/music-is/labels/arcanea/CAN
  [STRE] The registry + swarm-orchestrator routing design is genuinely well-engineered
        registry/agents.json (32 entries, portable/status flags), registry/tools.json, and agents/swarm-orchestrator.md together form a real routing layer: explicit disambiguation rules (personal-catalog vs. operated-label vs. public-vertical, ad-hoc vs. curriculum), 
  [STRE] music-brief-writer's single-writer-per-file, human-gated design is a sound answer to the multi-agent clobber problem at the brief layer
        The agent explicitly declares what it must NOT modify (sidecar JSON, audio, arcanea-guardians.json, MEDIA-TOOLING-DOCTRINE.md, any album.json), never sets guardian.confirmed:true itself, and is idempotent by slug. This ownership-boundary pattern — one agent, o
  [STRE] Deterministic eval harness gates model/engine choices before scaling
        evals/music/RUBRICS.md + score.mjs run canon-fit and mastering-pass checks with zero LLM calls before a batch job scales to a new album or engine, and WORKFLOW.md's Loop D ties this explicitly into the native-vs-frontier tool-routing decision rather than trust
-RECOMMENDATIONS-
  (hours) Rewrite FrankX/.claude/commands/create-music.md to actually dispatch @music-create-wizard (or explicitly retire the wizard and keep the lyric-genius flow as the one true implementation) — pick one, delete the other's independent logic.
        why: Right now the most-used entry point and the agent that claims to own it are two different, non-interoperating implementations; a user or a future agent session cannot trust the doc
  (hours) Move FrankX/.claude/agents/frequency-music-production.md and music-production.md into a deprecated/ folder with a one-line pointer to @music-producer / @music-suno-mastery (hide, don't delete, per the repo's own doctrine), and strip the '432/528 Hz healing' language before archiving.
        why: Two files literally named 'Music Producer' collide with the canonical orchestrator, are absent from the hub's own registry, and contain language both FrankX and ACOS explicitly ban
  (days) Build a small atomic-write helper (lockfile or a per-track sidecar JSON per asset type instead of shared-column writes) for catalog.csv / album.json, and require mv-artwork / mv-canvas / mv-render / music-mastering-qc to write through it instead of doing their own read-modify-write.
        why: This closes the one confirmed data-integrity gap: parallel cover/video/master dispatch (explicitly designed as parallel in WORKFLOW.md) has no protection against clobbering each ot
  (hours) Refresh music-intelligence-systems/agents/swarm-orchestrator.md's header counts (31->32 agents, 10->14 tools) and add the missing /music-suno-prompt entry to registry/tools.json's music-is-commands.provides list.
        why: The router that every ambiguous ask is supposed to go through is citing stale counts from its own registry two days out of date.
  (hours) Do one reconciliation pass on SYSTEM.md/WORKFLOW.md's module-8 status now that music-brief-writer + a passing fixture brief exist, and re-run validate-registry.mjs / the eval harness to confirm shipped status before the next doc snapshot.
        why: SYSTEM.md explicitly claims 'nothing here is aspirational unless a status label says so' — that claim is only true if the doc is re-checked after each build session, which didn't h
  (days) Add a version-stamp check (e.g. a hash or updated-date comparison) between Arcanea's CANON_LOCKED.md, Starlight's mirror, and MIS's arcanea-guardians.json, surfaced in the eval harness as a 'canon freshness' gate.
        why: The three-tier copy chain is a deliberate, reasonable design to avoid runtime cross-repo coupling, but nothing currently prevents the mirror from silently going stale if the Guardi
  (days) Extend tools/validate-registry.mjs to scan each registered repo's actual .claude/agents/*.md files and flag any agent not present in registry/agents.json (and vice versa).
        why: The registry's core value proposition is 'one hub, don't duplicate' — that promise is only trustworthy if drift like the two undocumented Music Producer agents gets caught automati
  (days) Consolidate the front-door command surface to: /create-music (guided single-track), /album (Arcanea pipeline: ingest/sequence/status/release), /music-triage (catalog scan->QC->stage loop), /music-song + /music-release (Starlight operated-label lifecycle), /music-video (single command with --format flag replacing mv-artwork/mv-canvas/mv-render/mv-visualizer as internal dispatch, keeping the mv-* files as thin deprecated aliases that print a pointer), and the swarm-orchestrator as the meta-router for anything ambiguous — 6-7 real front doors instead of the current ~17 independently-documented command files.
        why: Reduces the surface a user has to learn without deleting any working capability — the mv-* commands still exist as format-specific shortcuts underneath, per the repo's own 'fix nav
